import React from 'react';
// import Cart from '../Cart/Cart';
import './Random.css';

const Random = ({ chooseButton }) => {
    // console.log(() => chooseButton(cart));
    return (
        <div>
            <h1>this in random</h1>
        </div>
    );
};

export default Random;