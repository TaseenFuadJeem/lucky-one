import React from 'react';
import './Cart.css';

const Cart = (props) => {
    console.log(props);
    return (
        <div>
            <h1>this is a cart</h1>
        </div>
    );
};

export default Cart;